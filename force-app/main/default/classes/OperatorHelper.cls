public class OperatorHelper {
    public Decimal largestOutOfTwoNumbers(Decimal x, Decimal 
    y){
Decimal larDecNumber = math.max(x, y);
return larDecNumber;
    }
    public Decimal smallestOutOFNumbers(Decimal a, DEcimal b, Decimal c){
        Decimal x = math.min(a,b);
        
        Decimal minDecnumber = math.min(x.c);

        return minDecnumber;
    }
 
}
